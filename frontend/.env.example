# Frontend Environment Variables
# Copy this file to .env for local development

# Backend API URL (used by client-side code in the browser)
# For local development with Vite proxy, use the proxied path
VITE_API_URL=/api

# For direct backend connection (without proxy):
# VITE_API_URL=http://localhost:8000/api

# Backend Proxy Target (used by Vite dev server proxy configuration)
# This tells the Vite dev server where to forward /api requests
# Local development (running backend separately):
VITE_BACKEND_PROXY_TARGET=http://localhost:8000

# Docker Compose (using service name):
# VITE_BACKEND_PROXY_TARGET=http://backend:8000
